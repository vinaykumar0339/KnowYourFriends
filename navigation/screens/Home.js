import { Text, SafeAreaView } from 'react-native';
import React from 'react';
import { CustomIcons } from '../../resources';

const Home = () => {
    return (
        <SafeAreaView style={{flex: 1, justifyContent: 'space-between', alignItems: 'center'}}>
            <Text>Home screen</Text>
        </SafeAreaView>
    );
};

export default Home;
